# SmartScaling Restaurant RL (Workspace Entry Point)

The full project lives in `smartscaling_restaurant_rl/`.

You can still run the commands exactly as written in the prompt from this workspace root:

```bash
pip install -r requirements.txt
python training/train_rl.py
streamlit run dashboard/streamlit_app.py
```

If you prefer, open `smartscaling_restaurant_rl/README.md` for full documentation.

